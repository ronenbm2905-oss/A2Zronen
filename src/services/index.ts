/**
 * Domain services.
 *
 * One module per domain (`task.service.ts`, `project.service.ts`, ...), each
 * exposing plain functions that own the business rules. Route handlers stay
 * thin: parse input, call a service, return the result.
 *
 * Empty by design — this phase builds infrastructure only.
 */

export {};
