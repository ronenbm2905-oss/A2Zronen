/**
 * Shared React hooks.
 *
 * Client-side data access and UI state helpers land here (`use-auth.ts`,
 * `use-api.ts`, ...) and are re-exported from this barrel.
 *
 * Empty by design — this phase builds infrastructure only.
 */

export {};
